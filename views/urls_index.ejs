<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% include ./partials/_header %>  

  <p><% urls %></p>
  
  <% for (let i in urls) { %>
    <p><%= `${urls[i]}: ${i}` %></p><a href="/urls/<%= i %>">Edit</a>
    <form method="POST" action="/urls/<%= i %>/delete">
      <input type="submit" value="Delete URL">
    </form>
  <% } %>
  
  <a href="#">Shorten a URL!</a>
</body>
</html>