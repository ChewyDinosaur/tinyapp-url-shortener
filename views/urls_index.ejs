<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% include ./partials/_header %>  

  <p><% urls %></p>
  <ul>
    <% for (let i in urls) { %>
      <li><%= `${urls[i]}: ${i}` %></li>
    <% } %>
  </ul>
  <a href="#">Shorten a URL!</a>
</body>
</html>